# Examples

- `langgraph_basic.py` — Wrap a LangGraph agent with loop detection
- `openai_wrapper.py` — Wrap the OpenAI client with loop detection

Run with:
```bash
pip install tokencircuit[langgraph,openai]
python examples/langgraph_basic.py
```
