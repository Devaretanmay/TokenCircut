name: Keep Alive

on:
  schedule:
    - cron: "0 8 * * *"

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Supabase health endpoint
        run: curl -f ${{ secrets.SUPABASE_HEALTH_URL }} || echo "Ping failed"
        shell: bash
